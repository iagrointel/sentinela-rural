# Copie para .env e preencha com SUAS chaves. O .env NÃO é versionado.
# O diagnóstico (Sentinel-2, MapBiomas) NÃO precisa de chave — roda sobre dados públicos.
# Só o envio de mensagem usa a Meta Cloud API, com a SUA conta:

META_TOKEN=seu_token_aqui
META_PHONE_NUMBER_ID=seu_phone_number_id_aqui
