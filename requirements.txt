pystac-client>=0.7
rasterio>=1.3
shapely>=2.0
pyproj>=3.5
numpy>=1.24
pillow>=10.0
requests>=2.31
# ogr2ogr (GDAL CLI) é usado por sentinela/export_kml.py — instale o GDAL no sistema:
#   Debian/Ubuntu: sudo apt-get install gdal-bin
